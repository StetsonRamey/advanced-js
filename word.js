var Letter = require("./letter");

var Word = function (wordToGuess) {
    this.new = [];

    // function 1 here

    // need a function here that returns the string representing the word.  This should call the function on each letter object (the first function defined in Letter.js) that displays the character or an underscore and concatenate those together.

    // function 2 here

    // A function that takes a character as an argument and calls the guess function on each letter object (the second function defined in Letter.js)

}


// module.exports = Word; 